import * as React from 'react';
declare type Props = {
    children: React.ReactNode;
    title: string;
};
export default function FormElementComponent({ children, title }: Props): JSX.Element;
export {};
